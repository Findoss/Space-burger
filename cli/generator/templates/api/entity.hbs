import { schema } from 'normalizr';

const {{pascalCase name }}Schema = new schema.Entity('{{lowerCase name }}s');
export const {{pascalCase name }}sSchema = new schema.Array({{pascalCase name }}Schema);
