type {{pascalCase name }}Id = string;

export type {{pascalCase name }}Raw = {
  id: {{pascalCase name }}Id;
};
export type {{pascalCase name }}sRaw = {{pascalCase name }}Raw[];

export type {{pascalCase name }} = {
  id: {{pascalCase name }}Id;
};
export type {{pascalCase name }}s = {{pascalCase name }}[];

export type {{pascalCase name }}sEntity =
  | {
      entities: {
        {{ name }}?: {
          [id: string]: {{pascalCase name }};
        };
      };
      result: {{pascalCase name }}Id[];
    }
  | undefined;
