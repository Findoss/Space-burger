import React from 'react';
import { ComponentStory, ComponentMeta } from '@storybook/react';

import { Pure{{pascalCase name}} } from './{{pascalCase name}}';

export default {
  title: '{{#if isPage}}Pages{{else}}Components{{/if}}/Pure{{pascalCase name}}',
  component: Pure{{pascalCase name}},
} as ComponentMeta<typeof Pure{{pascalCase name}}>;

const Template: ComponentStory<typeof Pure{{pascalCase name}}> = (args) => (
  <Pure{{pascalCase name}} {...args} />
);

export const Default = Template.bind({});
Default.args = {
  className: 'class',
};
